# حالة التسجيل وتسجيل الدخول حسب الباكند

## هل يعمل؟

**يعمل** إذا كان الباكند يوفّر المسارات والحقول المذكورة أدناه. إن كان الباكند يرجّع **404** (مسار غير موجود) أو **422** (خطأ تحقق)، فالتسجيل/الدخول لن يكتملا حتى يطابق الباكند ما يلي أو نعدّل الطلب حسب ما يطلبه الباكند.

---

## الويب (React)

| ما يحدث | التفاصيل |
|---------|----------|
| **المسارات** | `POST /v1/ar/login` و `POST /v1/ar/register` (أو `en` بدل `ar` حسب لغة الواجهة) |
| **قبل الطلب** | طلب `GET /sanctum/csrf-cookie` ثم إرسال الكوكي تلقائياً مع الطلب |
| **نوع الجسم** | `application/x-www-form-urlencoded` (form) |
| **تسجيل الدخول** | `email`, `password`, `locale`, `language` |
| **التسجيل** | `name`, `full_name`, `email`, `password`, `password_confirmation`, `locale`, `language`, `phone`, `mobile` (اختياري) |
| **الهيدر** | `Accept-Language: ar` أو `en` |

**ليعمل حسب الباكند يجب:**
- وجود مسارين: `POST /v1/{ar|en}/login` و `POST /v1/{ar|en}/register`
- قبول الجسم بصيغة form (أو JSON إن غيّرنا الويب)
- عند النجاح: إرجاع `token` (واختياري `refresh_token`) في الاستجابة أو داخل `data`
- عند فشل التحقق (422): إرجاع حقل `errors` ليعرض الويب رسائل الحقول

---

## الموبايل (Flutter)

| ما يحدث | التفاصيل |
|---------|----------|
| **القاعدة** | `https://property-management-platform.onrender.com/api` |
| **المسارات** | `POST /api/auth/login` و `POST /api/auth/register` |
| **نوع الجسم** | `application/json` |
| **تسجيل الدخول** | `email`, `password`, `locale`, `language` |
| **التسجيل** | `name`, `email`, `password`, `password_confirmation`, `locale`, `language`, `phone`, `mobile`, `user_type` (اختياري) |
| **الهيدر** | `Accept-Language: ar` أو `en` (من لغة التطبيق) |

**ليعمل حسب الباكند يجب:**
- وجود مسارين: `POST /api/auth/login` و `POST /api/auth/register`
- قبول JSON
- نفس شكل الاستجابة (نجاح: `token`، فشل: `message` و `errors`)

---

## الفرق بين الويب والموبايل

| | الويب | الموبايل |
|--|--------|----------|
| **المسار** | `/v1/ar/login` و `/v1/ar/register` | `/api/auth/login` و `/api/auth/register` |
| **الشكل** | form-urlencoded | JSON |

إذا كان الباكند يوفّر **فقط** مسارات `/v1/...` فالموبايل يحتاج أن يوجّه طلباته إلى نفس المسارات (تغيير القاعدة والمسار في الموبايل).  
إذا كان يوفّر **فقط** مسارات `/api/auth/...` فالويب يمكن ضبطه بـ `REACT_APP_USE_API_AUTH=true` في `.env` ليستخدم نفس المسارات.

---

## خلاصة

- **من جهة التطبيقين:** التسجيل وتسجيل الدخول **مربوطان حسب الباكند** من ناحية الحقول (`name`, `email`, `password`, `password_confirmation`, `locale`, `language`, `phone`/`mobile`) والهيدر `Accept-Language`.
- **أن يعملا فعلياً** يتطلّب أن الباكند:
  1. يوفّر أحد النوعين: إما مسارات **v1** أو مسارات **api/auth**.
  2. يقبل الحقول أعلاه ولا يطلب حقولاً إلزامية غير مرسلة.
  3. يرجّع عند النجاح `token` (واختياري `refresh_token`).

إذا استمر **422** من الباكند، مطلوب معرفة تفاصيل التحقق من الباكند (أي حقول مطلوبة أو قواعد) ثم إما تعديل الباكند أو تعديل الويب/الموبايل ليرسلان نفس ما يتوقعه الباكند.
