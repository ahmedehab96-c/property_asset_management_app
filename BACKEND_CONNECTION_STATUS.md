# حالة ربط الباكند بالويب والموبايل

## نعم — الباكند مربوط بالاثنين

كلا التطبيقين (الويب والموبايل) مُعدّان للاتصال **بنفس سيرفر الباكند**:

| | الويب (React) | الموبايل (Flutter) |
|--|----------------|---------------------|
| **السيرفر** | `https://property-management-platform.onrender.com` | نفس الرابط |
| **المصدر** | `web_app/src/services/api.js` + `package.json` (proxy) | `mobile_app/lib/config/api_config.dart` |

---

## تفاصيل الربط

### 1) تطبيق الويب (الويب أب)

- **الرابط الأساسي**: `https://property-management-platform.onrender.com`  
  (أو من `.env`: `REACT_APP_API_BASE_URL`)
- **في التطوير**: الطلبات تمر عبر **proxy** في `package.json` إلى نفس السيرفر.
- **المصادقة (تسجيل الدخول / التسجيل)**:
  - **افتراضي**: مسارات حسب اللغة مثل `/v1/ar/login`, `/v1/ar/register`, `/v1/en/login` …
  - **بديل**: إذا وُضع في `.env`: `REACT_APP_USE_API_AUTH=true` تُستخدم المسارات: `/api/auth/login`, `/api/auth/register`.

### 2) تطبيق الموبايل (الذي اشتغلته المبرمجة)

- **الرابط الأساسي**: `https://property-management-platform.onrender.com/api`  
  (أو من `.env`: `API_BASE_URL`)
- **المصادقة**: يستخدم مسارات تحت `/api`:
  - تسجيل الدخول: `POST /api/auth/login`
  - التسجيل: `POST /api/auth/register`
  - تسجيل الخروج: `POST /api/auth/logout`
  - المستخدم الحالي: `GET /api/auth/user`
  - وغيرها (نسيت كلمة المرور، تحديث التوكن، إلخ).

كل الطلبات تذهب إلى:  
`https://property-management-platform.onrender.com` + المسار (مثلاً `/api/auth/login`).

---

## الفرق بين الويب والموبايل (المسارات فقط)

| الخدمة | الويب (افتراضي) | الموبايل |
|--------|------------------|----------|
| تسجيل الدخول | `POST /v1/ar/login` أو `POST /v1/en/login` | `POST /api/auth/login` |
| التسجيل | `POST /v1/ar/register` أو `POST /v1/en/register` | `POST /api/auth/register` |

- **الويب** يستخدم مسارات مُنسقة مع اللغة (`/v1/{ar|en}/...`) ويرسل `Accept-Language`.
- **الموبايل** يستخدم مسارات ثابتة تحت `/api/auth/...`.

الباكند يجب أن يدعم **أحد الخيارين أو الاثنين** حسب ما تم برمجته:
- إن كان يدعم فقط `/api/auth/*` → الويب يعمل إذا ضُبط `REACT_APP_USE_API_AUTH=true`.
- إن كان يدعم فقط `/v1/ar/login` و`/v1/ar/register` → الموبايل يحتاج أن تُغيّر المبرمجة الـ base URL والمسارات لتناسب الباكند، أو الباكند يضيف مسارات `/api/auth/*`.

---

## خلاصة

- **هل الباكند مربوط بالويب؟** نعم — الويب يرسل الطلبات إلى نفس السيرفر (مع proxy في التطوير).
- **هل الباكند مربوط بالموبايل؟** نعم — الموبايل مُعدّ لاستخدام نفس السيرفر مع بادئة `/api`.
- **هل الاشتغال الفعلي مضمون؟** يعتمد على ما ينفّذه الباكند بالضبط (مسارات `/v1/...` و/أو `/api/auth/...`). إن كانت المسارات في الباكند تطابق ما يطلبه كل تطبيق، فالتسجيل وتسجيل الدخول يعملان من الويب والموبايل.
