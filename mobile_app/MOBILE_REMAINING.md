# ما تبقى — موبايل / منصة

لا بنود إلزامية متبقية.

## اختياري للإنتاج فقط

- `MAIL_MAILER=smtp` + بيانات مزوّد البريد
- `OPENAI_API_KEY` لـ Vision أقوى من heuristics

## جاهز

- قوائم بدون demo عند فشل الشبكة (قائمة فارغة + `fromDemo: false`)
- بريد تحقق/إعادة تعيين + تحليل صور على الخادم
- Filament `/admin` — بدون Flutter Web
